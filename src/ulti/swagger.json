{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "Report_work API",
    "description": ""
  },
  "host": "report-work-mp9x.onrender.com",
  "tags": [
    {
      "name": "Users"
    }
  ],
  "securityDefinitions": {
    "Bearer": {
      "type": "apiKey",
      "description": "JWT Authorization header using the Bearer scheme. \\r\\n\\r\\n\r\n            Enter 'Bearer' [space] and then your token in the text input below.\r\n            \\r\\n\\r\\nExample: 'Bearer 12345abcdef'",
      "name": "Authorization",
      "in": "header"
    }
  },

  "security": [
    {
      "Bearer": []
    }
  ],
  "paths": {
    "/user/create": {
      "post": {
        "summary": "add user",
        "tags": ["Admin"],
        "description": "Admin add user",
        "parameters": [
          {
            "name": "user information",
            "in": "body",
            "description": "Admin add user",
            "schema": {
              "$ref": "#/definitions/User"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "201": {
            "description": "Add user success",
            "schema": {
              "$ref": "#/definitions/User"
            }
          },
          "500": {
            "description": "Add user failed!!"
          }
        }
      }
    },
    "/export/download": {
      "post": {
        "summary": "Export report",
        "tags": ["Admin"],
        "description": "Admin export report",
        "parameters": [
          {
            "name": "date",
            "in": "body",
            "description": "Date to export report",
            "schema": {
              "$ref": "#/definitions/ReportDownload"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "200": {
            "description": "Downloaded excel file successfully"
          },
          "404": {
            "description": "No reports found for this date!!"
          },
          "500": {
            "description": "Error while generating report!!"
          }
        }
      }
    },
    "/login": {
      "post": {
        "summary": "Login",
        "tags": ["Authentication"],
        "description": "User login",
        "parameters": [
          {
            "name": "username",
            "in": "body",
            "description": "Username of user",
            "schema": {
              "$ref": "#/definitions/Login"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "200": {
            "description": "Login successfully"
          },
          "404": {
            "description": "No user found!!"
          },
          "500": {
            "description": "Error when login!!"
          }
        }
      }
    },
    "/export": {
      "get": {
        "summary": "view report",
        "tags": ["Admin"],
        "description": "View Report information",
        "parameters": [
          {
            "name": "date",
            "in": "query",
            "description": "Date to export report",
            "schema": {
              "type": "Date"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "200": {
            "description": "Login successfully",
            "schema": {
              "$ref": "#/definitions/Report"
            }
          },
          "500": {
            "description": "Error server when searching for report!!"
          }
        }
      }
    },
    "/report/input": {
      "post": {
        "summary": "Submit report",
        "tags": ["Users"],
        "description": "User input report",
        "parameters": [
          {
            "name": "report",
            "in": "body",
            "description": "report's information",
            "schema": {
              "$ref": "#/definitions/Report"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "200": {
            "description": "Login successfully",
            "schema": {
              "$ref": "#/definitions/Report"
            }
          },
          "500": {
            "description": "Error server when searching for report!!"
          }
        }
      }
    },
    "/report": {
      "get": {
        "summary": "Form submit report",
        "tags": ["Users"],
        "description": "User input report",
        "parameters": [
          {
            "name": "date",
            "in": "query",
            "description": "report's date",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "idUser",
            "in": "query",
            "description": "user's id",
            "schema": {
              "type": "string"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "200": {
            "description": "Login successfully",
            "schema": {
              "$ref": "#/definitions/Report"
            }
          },
          "500": {
            "description": "Error server when searching for report!!"
          }
        }
      }
    },
    "/user": {
      "get": {
        "summary": "get all user",
        "tags": ["Admin"],
        "description": "User's information",
        "responses": {
          "200": {
            "description": "Login successfully",
            "schema": {
              "$ref": "#/definitions/AllUser"
            }
          },
          "500": {
            "description": "lỗi sever"
          }
        }
      }
    },
    "/user/edit": {
      "put": {
        "summary": "Edit user",
        "tags": ["Admin"],
        "description": "Edit user's information",
        "parameters": [
          {
            "name": "user",
            "in": "body",
            "description": " user's information",
            "schema": {
              "$ref": "#/definitions/EditUser"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "200": {
            "description": "Login successfully",
            "schema": {
              "$ref": "#/definitions/EditUser"
            }
          },
          "500": {
            "description": "Error server when edit user!!"
          }
        }
      }
    },
    "/user/detail": {
      "get": {
        "summary": "User's information",
        "tags": ["Admin"],
        "description": "User's information",
        "parameters": [
          {
            "name": "idUser",
            "in": "query",
            "description": " user's information",
            "schema": {
              "type": "string"
            }
          }
        ],
        "produces": ["application/json"],
        "responses": {
          "200": {
            "description": "Show user's information ",
            "schema": {
              "$ref": "#/definitions/EditUser"
            }
          },
          "500": {
            "description": "Error server when show user's information!!"
          }
        }
      }
    }
  },

  "definitions": {
    "EditUser": {
      "properties": {
        "id": {
          "type": "string"
        },
        "username": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "msnv": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "password": {
          "type": "string"
        }
      }
    },
    "AllUser": {
      "properties": {
        "username": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "msnv": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "role": {
          "type": "string"
        }
      }
    },
    "User": {
      "properties": {
        "username": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "password": {
          "type": "string"
        }
      }
    },
    "Login": {
      "properties": {
        "username": {
          "type": "string"
        },
        "password": {
          "type": "string"
        }
      }
    },
    "Report": {
      "properties": {
        "idUser": {
          "type": "string"
        },
        "msnv": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "date": {
          "type": "string"
        },
        "today": {
          "type": "string"
        },
        "tomorrow": {
          "type": "string"
        }
      }
    },
    "ReportDownload": {
      "properties": {
        "date": {
          "type": "string"
        },
        "note": {
          "type": "string"
        }
      }
    }
  }
}
